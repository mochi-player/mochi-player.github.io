# Site settings
title: mochi-player
program_name: mochi-player
description: The libmpv based media player
url: "http://mochi-player.github.io/" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
exclude:
  - Gemfile
  - Gemfile.lock
sass:
  sass_dir: _sass

# for Travis
exclude: [vendor]
sudo: false

# jekyll serve settings
host: 0.0.0.0
